# Lidar Filter

Filtering cloud points from Lidar Sensor.

## Setup
Open the directory in CMD and run the following commands:
```
mkdir build

cd build

cmake ..

cd ..

cmake --build build

cd build/debug

./filter
```